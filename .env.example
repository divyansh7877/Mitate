# ============================================================================
# Appwrite Configuration (Frontend)
# ============================================================================
VITE_APPWRITE_ENDPOINT=https://nyc.cloud.appwrite.io/v1
VITE_APPWRITE_PROJECT_ID=your_project_id_here
VITE_APPWRITE_FUNCTION_GENERATE_ID=generate-poster-func

# Optional: Use mock data instead of real API calls (for development)
VITE_USE_MOCKS=false

# ============================================================================
# Appwrite Functions Environment Variables
# (Configure these in Appwrite Console for each function)
# ============================================================================

# Required for both functions:
APPWRITE_API_KEY=your_api_key_here
DATABASE_ID=mitate-db
BUCKET_ID=poster-images

# Required for process-generation function:
FIBO_API_KEY=your_fibo_api_key_here
DO_GRADIENT_API_KEY=your_digitalocean_gradient_api_key_here
DO_GRADIENT_MODEL=llama3.3-70b-instruct

# Required for generate-poster function:
WORKER_FUNCTION_ID=your_process_generation_function_id_here

# ============================================================================
# Notes
# ============================================================================
# 1. Frontend env vars must be prefixed with VITE_ to be accessible
# 2. Function env vars are configured in appwrite.config.json and Appwrite Console
# 3. Get FIBO API key from: https://bria.ai
# 4. Get DigitalOcean Gradient API key from: https://cloud.digitalocean.com/account/api/tokens
#    (Use MODEL ACCESS KEY, not regular API token)
